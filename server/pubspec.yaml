name: notally_server
description: Notally's self-hosted, E2EE-blind sync server (shelf + SQLite).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.0

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  sqlite3: ^2.4.6
  notally_core:
    path: ../packages/notally_core

dev_dependencies:
  test: ^1.25.0
  http: ^1.2.0
